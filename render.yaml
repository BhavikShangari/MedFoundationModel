services:
  - type: web
    name: brset-review
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn brset_review_app:app --bind 0.0.0.0:$PORT --workers 1 --threads 2 --timeout 120
    envVars:
      - key: BRSET_REVIEW_SECRET_KEY
        generateValue: true
